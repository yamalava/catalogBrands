const initialStore = {
    userProfile: undefined,
}

export function userReducer(state = initialStore, action) {
    switch(action.type) {
        default: {
            return state;
        }
    }
}